{
  "name": "@eligian/language",
  "description": "The language specific package",
  "version": "0.0.1",
  "type": "module",
  "engines": {
    "node": ">=20.10.0",
    "npm": ">=10.2.3"
  },
  "files": [
    "dist",
    "out",
    "src"
  ],
  "main": "./dist/index.cjs",
  "types": "./out/index.d.ts",
  "exports": {
    ".": {
      "types": "./out/index.d.ts",
      "default": "./dist/index.cjs"
    },
    "./css-service": {
      "types": "./out/css/css-service.d.ts",
      "default": "./dist/css/css-service.cjs"
    },
    "./errors": {
      "types": "./out/errors/index.d.ts",
      "default": "./dist/errors/index.cjs"
    }
  },
  "typesVersions": {
    "*": {
      ".": [
        "out/index"
      ]
    }
  },
  "scripts": {
    "clean": "shx rm -fr *.tsbuildinfo out dist coverage",
    "generate:metadata": "tsx src/completion/generate-metadata.ts",
    "generate:registry": "tsx src/compiler/operations/generate-registry.ts",
    "generate": "pnpm run generate:metadata && pnpm run generate:registry && pnpm run langium:generate",
    "build": "pnpm run generate && node esbuild.mjs && tsc -p tsconfig.src.json --emitDeclarationOnly",
    "build:clean": "pnpm run clean && pnpm run build",
    "langium:generate": "langium generate",
    "langium:watch": "langium generate --watch",
    "test": "vitest run --reporter=dot --hideSkippedTests",
    "test:coverage:ci": "vitest run --coverage"
  },
  "dependencies": {
    "@eligian/shared-utils": "workspace:*",
    "css-tree": "3.1.0",
    "domhandler": "5.0.3",
    "domutils": "3.2.2",
    "fast-check": "4.3.0",
    "htmlparser2": "10.0.0",
    "langium": "4.0.3",
    "postcss": "8.5.6",
    "postcss-selector-parser": "7.1.0",
    "typir": "0.3.0",
    "typir-langium": "0.3.0"
  },
  "devDependencies": {
    "@types/css-tree": "2.3.11",
    "langium-cli": "4.0.0"
  }
}
