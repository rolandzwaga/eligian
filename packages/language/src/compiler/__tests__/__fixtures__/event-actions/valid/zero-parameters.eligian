// Zero Parameters Fixture (T015)
// Feature 028 - User Story 1: Event action with no parameters

// Define helper action
action init() [
  selectElement("#app")
]

// Event action with zero parameters
on event "app-ready" action Initialize() [
  selectElement(".app-container")
  addClass("ready")
]

// Timeline required by compiler
timeline "demo" in "#app" using raf {
  at 0s..1s init()
}
