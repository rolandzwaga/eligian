{
  "name": "@eligian/cli",
  "description": "The cli specific package",
  "version": "0.0.1",
  "type": "module",
  "engines": {
    "node": ">=20.10.0",
    "npm": ">=10.2.3"
  },
  "files": [
    "dist",
    "src"
  ],
  "bin": {
    "eligian-cli": "./dist/cli.mjs"
  },
  "scripts": {
    "clean": "shx rm -fr *.tsbuildinfo out dist",
    "build": "node esbuild.mjs",
    "build:clean": "pnpm run clean && pnpm run build",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@eligian/language": "workspace:*",
    "chalk": "5.6.2",
    "commander": "14.0.2"
  }
}
